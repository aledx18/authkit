---
"@aledx18/authkit": minor
---

Style the generated auth pages with Tailwind: signin, register, forgot-password, reset-password and dashboard now ship with a consistent card layout (slate background, blue primary, green/red message banners) and import the Tailwind stylesheet. The example mirrors the styled scaffold.
