---
"@aledx18/supabase-auth-react": patch
---

Make package self-contained: moved `getAuthErrorMessage` and `AuthResult` locally, removed the dependency on `@aledx18/supabase-auth-core`. Package remains paused.
