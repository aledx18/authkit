---
"@aledx18/astro": minor
---

Add `requireGuest` to `@aledx18/astro/protect` — the inverse of `requireAuth`: redirects authenticated users away from guest-only pages (signin, register) to `/dashboard` by default.
