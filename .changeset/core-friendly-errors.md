---
"@aledx18/supabase-auth-core": minor
---

Add `getAuthErrorMessage` — maps Supabase auth error codes to friendly, UI-ready messages (falls back to the raw message for unknown codes). Framework-agnostic, shared by the Astro actions.
