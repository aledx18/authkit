# @aledx18/supabase-auth-react

**PAUSADO**

Este paquete está pausado. No se aceptan cambios ni se le agrega funcionalidad hasta que:

1. `@aledx18/supabase-auth-core` tenga una superficie estable
2. `@aledx18/astro` esté maduro y probado en producción

La regla de oro: primero JS puro (core + astro), después React. Nunca al revés.
